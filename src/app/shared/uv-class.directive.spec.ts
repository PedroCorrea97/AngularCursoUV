import { UvClassDirective } from './uv-class.directive';

describe('UvClassDirective', () => {
  it('should create an instance', () => {
    const directive = new UvClassDirective();
    expect(directive).toBeTruthy();
  });
});
